import { GClient } from '../GClient';
export declare class Plugin {
    readonly name: string;
    readonly run: (client: GClient) => any;
    constructor(name: string, run: (client: GClient) => any);
    static validate(plugin: Plugin): boolean | void;
}
