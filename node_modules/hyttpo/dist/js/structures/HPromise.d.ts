import { HPromiseEvents } from '../util/constants';
export declare class HPromise<T> extends Promise<T> {
    private _on;
    private _emit;
    constructor(executor: any);
    on<K extends keyof HPromiseEvents>(event: K, listener: (args: HPromiseEvents[K]) => void): any;
    emit(name: any, ...args: any[]): any;
}
