/// <reference types="node" />
import { PayloadRequest } from './constants';
declare class Util {
    static isJSON(data: any): boolean;
    static isObject(data: any): boolean;
    static isFunction(data: any): boolean;
    static isStream(data: any): boolean;
    static isFormData(data: any): boolean;
    static stringTrim(data: any): string;
    static toString(data: any): string;
    static toArrayBuffer(buf: any): ArrayBuffer;
    static parseHeaders(headers: any): object;
    static responseRefactor(data: Buffer | string, encoding?: BufferEncoding): string | Buffer;
    static dataConfigParse(data: PayloadRequest): PayloadRequest;
    static stringsToLowerCase<T>(object: {
        [key: string]: T;
    }): {
        [key: string]: T;
    };
}
export default Util;
